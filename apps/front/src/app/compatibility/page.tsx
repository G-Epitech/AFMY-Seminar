import { AutoComplete } from "@/components/ui/autocomplete";
import { generator } from "@/lib/generator";

export default function CompatibilityPage() {
    const customers = generator.customers();

    return (
        <main>
            <div>
                
            </div>
        </main>
    );
}
